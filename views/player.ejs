<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= player ? player.username : 'Player Not Found' %> - RankTiers</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="container"><a href="/">&larr; Back</a></header>
  <main class="container">
    <% if (!player) { %>
      <p>Player not found.</p>
    <% } else { %>
      <h2><%= player.username %></h2>
      <p>Points: <%= player.points %></p>
      <p>Games: <%= (player.games || []).join(', ') %></p>
    <% } %>
  </main>
</body>
</html>
